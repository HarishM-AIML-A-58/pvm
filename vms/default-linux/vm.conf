# VM Configuration Overrides
# Leave values blank or comment them out to inherit global auto-detected settings

name=Default Linux VM
# memory_mb=4096
# cores=4
display=sdl
network=nat
ssh_port=2222
uefi=false
# disk=disk.qcow2
